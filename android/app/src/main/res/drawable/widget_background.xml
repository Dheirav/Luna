<?xml version="1.0" encoding="utf-8"?>
<!--
    Widget background. `system_app_widget_background_radius` is the launcher's own corner radius,
    so the widget matches whatever shape the phone uses rather than guessing at one — Xiaomi,
    Samsung and Pixel all differ. Available from API 31, which is minSdk.
-->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <corners android:radius="@android:dimen/system_app_widget_background_radius" />
    <solid android:color="@color/widget_background" />
</shape>
